<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>선택한 글</title>

    <!-- jquery -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.min.js"></script>

    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">

    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8" crossorigin="anonymous"></script>

    <!-- main js -->

    <!-- css -->
    <link rel="stylesheet" href="../css/style.css">

    <style>
        #detail {
            position: relative;
            width: 100%;
            height: 100vh;
        }
        .container {
            max-width: 800px;
            height: 100%;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }


        #content {
            height: 300px;
        }

        .btn {
            float: right;
        }
    </style>
</head>
<body id="detail">
    <div class="container bg-light"> 
        <form action="/board/detail/<%= ARTICLE.id %>" method="POST">
            <div class="card rounded-0 mt-5">
                <div class="card-header bg-transparent" id="title">
                    <h5>[제목] <%= ARTICLE.title %></h5>
                    
                    <div class="d-flex justify-content-between">
                        <div>
                            <span>작성자</span>
                            <span>시간</span>
                        </div>
                        
                        <div>
                            <span>조회수</span>
                            <span>추천</span>
                            <span>댓글</span>
                        </div>
                    </div>
                    
                </div>
                
                <div class="card-body">
                  <p class="card-title" id="content"><%= ARTICLE.content %></p>
                
                </div>
                <div class="card-footer bg-transparent" id="comment">댓글
                    
                </div>
            </div>
        </form>

        <% if (ARTICLE.id === userId) { %>
            <a href="/board/delete"><button type="button" class="btn btn-danger rounded-0 mt-3">삭제</button></a>
            <a href="/board/emend/<%= ARTICLE._id %>"><button type="button" class="btn btn-success mt-3 me-3 rounded-0">수정</button></a>
        <% } else { %>
            <a href="/board/delete"><button type="button" class="btn btn-danger rounded-0 mt-3" disabled>삭제</button></a>
            <a href="/board/emend/<%= ARTICLE.id %>"><button type="button" class="btn btn-success mt-3 me-3 rounded-0" disabled>수정</button></a>
        <% } %>
    </div>
</body>
</html>