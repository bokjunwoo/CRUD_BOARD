<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>main</title>

    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">

    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8" crossorigin="anonymous"></script>

    <style>
      #board {
        position: relative;
        width: 100%;
        height: 100vh;
      }

      .container {
        max-width: 800px;
        height: 100%;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }

      .btn {
        float: right;
      }
    </style>
</head>
<body id="board">
    <div class="container bg-light">
        <h1 class="p-5 text-center">전체글</h1>
        <% if (userId !== undefined) { %>
          <a href="/board/write"><button type="button" class="btn btn-secondary mb-3 rounded-0">글쓰기</button></a>
        <% } else { %>
          <a href="/board/write"><button type="button" class="btn btn-secondary mb-3 rounded-0" disabled>글쓰기</button></a>
        <% } %>
        <table class="table table-bordered">
            <thead>
              <tr>
                <th scope="col" class="col-1 text-center">번호</th>
                <th scope="col" class="col-8 text-center">제목</th>
                <th scope="col" class="col-2 text-center">글쓴이</th>
                <th scope="col" class="col-1 text-center">추천</th>
              </tr>
            </thead>
            <tbody>
              <% if (articleCounts > 0) { %>
                <% for(let i=0; i < articleCounts; i++) { %>
              <tr>
                <th scope="row" class="text-center"><%= ARTICLE[i]._id %></th>
                <td onclick="location.href='/board/detail/<%= ARTICLE[i]._id %>'" style="cursor:pointer"><%= ARTICLE[i].title %></td>
                <td><%= ARTICLE[i].name %></td>
                <td class="text-center">0</td>
              </tr>
                <% } %>
              <% } else {%>
                <tr>
                  <td colspan="4" scope="row" class="text-center">게시글이 없습니다</td>
                </tr>
              <% } %>
            </tbody>
          </table>
    </div>
</body>
</html>